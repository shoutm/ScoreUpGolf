<%= form_for(@green) do |f| %>
  <% if @green.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@green.errors.count, "error") %> prohibited this green from being saved:</h2>

      <ul>
      <% @green.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
