<%= form_for([:scaffold, @golf_field]) do |f| %>
  <% if @golf_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@golf_field.errors.count, "error") %> prohibited this golf_field from being saved:</h2>

      <ul>
      <% @golf_field.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :region %><br />
    <%= f.number_field :region %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
